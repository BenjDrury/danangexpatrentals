{
  "name": "danangexpatrentals",
  "private": true,
  "workspaces": [
    "website",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=website",
    "build": "npm run build --workspace=backend && npm run build --workspace=website",
    "start": "npm run start --workspace=website"
  }
}
